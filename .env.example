# MongoDB Atlas Configuration
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority&appName=NewsLens
MONGO_DB=newslens

# Application Configuration
APP_NAME=newslens
ENVIRONMENT=development
DEBUG=True

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging Configuration
LOG_LEVEL=DEBUG
LOG_FORMAT=json

# Scraping Configuration
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36
REQUEST_TIMEOUT=30
MAX_RETRIES=3 

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017  # MongoDB connection string
MONGO_DB=newslens                    # Database name

# AWS Configuration
AWS_ACCESS_KEY_ID=                   # AWS access key for S3 access
AWS_SECRET_ACCESS_KEY=               # AWS secret key for S3 access
AWS_DEFAULT_REGION=us-east-1         # AWS region (e.g., us-east-1)
S3_BUCKET_NAME=                      # S3 bucket for storing screenshots

# Scraper Configuration
USER_AGENT="NewsLensBot/0.1"        # Bot identifier for requests

# Logging Configuration (Optional)
LOG_LEVEL=INFO                       # Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_FORMAT=json                      # Log format (json or text)

# Performance Configuration (Optional)
MAX_CONCURRENT_SNAPSHOTS=5           # Maximum number of concurrent snapshot processes
SCREENSHOT_TIMEOUT_SECONDS=120       # Maximum time to wait for screenshot capture